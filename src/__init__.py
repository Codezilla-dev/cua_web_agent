"""Computer-use capability system — Phase 1 (discovery loop).

Package is importable as `src` so the CLI runs via `python -m src.cli`.
Phase 1 builds one `observe -> decide -> guard -> act -> settle -> verify` loop
that completes a single real goal against a local target app and writes a trace.
"""
